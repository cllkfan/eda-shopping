<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eda.dao.TbItemDao">

    <resultMap type="com.eda.entity.TbItem" id="TbItemMap">
        <result property="gid" column="gid" jdbcType="INTEGER"/>
        <result property="title" column="title" jdbcType="VARCHAR"/>
        <result property="sellPoint" column="sell_point" jdbcType="VARCHAR"/>
        <result property="price" column="price" jdbcType="INTEGER"/>
        <result property="num" column="num" jdbcType="INTEGER"/>
        <result property="barcode" column="barcode" jdbcType="VARCHAR"/>
        <result property="image" column="image" jdbcType="VARCHAR"/>
        <result property="cid" column="cid" jdbcType="INTEGER"/>
        <result property="status" column="status" jdbcType="INTEGER"/>
        <result property="created" column="created" jdbcType="TIMESTAMP"/>
        <result property="updated" column="updated" jdbcType="TIMESTAMP"/>
        <result property="test1" column="test1" jdbcType="VARCHAR"/>
        <result property="test2" column="test2" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="TbItemMap">
        select gid,
               title,
               sell_point,
               price,
               num,
               barcode,
               image,
               cid,
               status,
               created,
               updated,
               test1,
               test2
        from tb_item
        where gid = #{gid}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="TbItemMap">
        select
        gid, title, sell_point, price, num, barcode, image, cid, status, created, updated, test1, test2
        from tb_item
        <where>
            <if test="gid != null">
                and gid = #{gid}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="sellPoint != null and sellPoint != ''">
                and sell_point = #{sellPoint}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="num != null">
                and num = #{num}
            </if>
            <if test="barcode != null and barcode != ''">
                and barcode = #{barcode}
            </if>
            <if test="image != null and image != ''">
                and image = #{image}
            </if>
            <if test="cid != null">
                and cid = #{cid}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="created != null">
                and created = #{created}
            </if>
            <if test="updated != null">
                and updated = #{updated}
            </if>
            <if test="test1 != null and test1 != ''">
                and test1 = #{test1}
            </if>
            <if test="test2 != null and test2 != ''">
                and test2 = #{test2}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from tb_item
        <where>
            <if test="gid != null">
                and gid = #{gid}
            </if>
            <if test="title != null and title != ''">
                and title = #{title}
            </if>
            <if test="sellPoint != null and sellPoint != ''">
                and sell_point = #{sellPoint}
            </if>
            <if test="price != null">
                and price = #{price}
            </if>
            <if test="num != null">
                and num = #{num}
            </if>
            <if test="barcode != null and barcode != ''">
                and barcode = #{barcode}
            </if>
            <if test="image != null and image != ''">
                and image = #{image}
            </if>
            <if test="cid != null">
                and cid = #{cid}
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="created != null">
                and created = #{created}
            </if>
            <if test="updated != null">
                and updated = #{updated}
            </if>
            <if test="test1 != null and test1 != ''">
                and test1 = #{test1}
            </if>
            <if test="test2 != null and test2 != ''">
                and test2 = #{test2}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="gid" useGeneratedKeys="true">
        insert into tb_item(title, sell_point, price, num, barcode, image, cid, status, created, updated, test1, test2)
        values (#{title}, #{sellPoint}, #{price}, #{num}, #{barcode}, #{image}, #{cid}, #{status}, #{created},
                #{updated}, #{test1}, #{test2})
    </insert>

    <insert id="insertBatch" keyProperty="gid" useGeneratedKeys="true">
        insert into tb_item(title, sell_point, price, num, barcode, image, cid, status, created, updated, test1, test2)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.sellPoint}, #{entity.price}, #{entity.num}, #{entity.barcode}, #{entity.image},
            #{entity.cid}, #{entity.status}, #{entity.created}, #{entity.updated}, #{entity.test1}, #{entity.test2})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="gid" useGeneratedKeys="true">
        insert into tb_item(title, sell_point, price, num, barcode, image, cid, status, created, updated, test1, test2)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.title}, #{entity.sellPoint}, #{entity.price}, #{entity.num}, #{entity.barcode}, #{entity.image},
            #{entity.cid}, #{entity.status}, #{entity.created}, #{entity.updated}, #{entity.test1}, #{entity.test2})
        </foreach>
        on duplicate key update
        title = values(title),
        sell_point = values(sell_point),
        price = values(price),
        num = values(num),
        barcode = values(barcode),
        image = values(image),
        cid = values(cid),
        status = values(status),
        created = values(created),
        updated = values(updated),
        test1 = values(test1),
        test2 = values(test2)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update tb_item
        <set>
            <if test="title != null and title != ''">
                title = #{title},
            </if>
            <if test="sellPoint != null and sellPoint != ''">
                sell_point = #{sellPoint},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="num != null">
                num = #{num},
            </if>
            <if test="barcode != null and barcode != ''">
                barcode = #{barcode},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="cid != null">
                cid = #{cid},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="created != null">
                created = #{created},
            </if>
            <if test="updated != null">
                updated = #{updated},
            </if>
            <if test="test1 != null and test1 != ''">
                test1 = #{test1},
            </if>
            <if test="test2 != null and test2 != ''">
                test2 = #{test2},
            </if>
        </set>
        where gid = #{gid}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from tb_item
        where gid = #{gid}
    </delete>

</mapper>

