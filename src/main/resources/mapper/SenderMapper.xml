<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eda.dao.SenderDao">

    <resultMap type="com.eda.entity.Sender" id="SenderMap">
        <result property="sId" column="s_id" jdbcType="INTEGER"/>
        <result property="gid" column="gid" jdbcType="INTEGER"/>
        <result property="sAddress" column="s_address" jdbcType="VARCHAR"/>
        <result property="sPhone" column="s_phone" jdbcType="VARCHAR"/>
        <result property="sName" column="s_name" jdbcType="VARCHAR"/>
        <result property="test1" column="test1" jdbcType="VARCHAR"/>
        <result property="test2" column="test2" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="SenderMap">
        select s_id,
               gid,
               s_address,
               s_phone,
               s_name,
               test1,
               test2
        from sender
        where s_id = #{sId}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="SenderMap">
        select
        s_id, gid, s_address, s_phone, s_name, test1, test2
        from sender
        <where>
            <if test="sId != null">
                and s_id = #{sId}
            </if>
            <if test="gid != null">
                and gid = #{gid}
            </if>
            <if test="sAddress != null and sAddress != ''">
                and s_address = #{sAddress}
            </if>
            <if test="sPhone != null and sPhone != ''">
                and s_phone = #{sPhone}
            </if>
            <if test="sName != null and sName != ''">
                and s_name = #{sName}
            </if>
            <if test="test1 != null and test1 != ''">
                and test1 = #{test1}
            </if>
            <if test="test2 != null and test2 != ''">
                and test2 = #{test2}
            </if>
        </where>
        limit #{pageable.offset}, #{pageable.pageSize}
    </select>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Long">
        select count(1)
        from sender
        <where>
            <if test="sId != null">
                and s_id = #{sId}
            </if>
            <if test="gid != null">
                and gid = #{gid}
            </if>
            <if test="sAddress != null and sAddress != ''">
                and s_address = #{sAddress}
            </if>
            <if test="sPhone != null and sPhone != ''">
                and s_phone = #{sPhone}
            </if>
            <if test="sName != null and sName != ''">
                and s_name = #{sName}
            </if>
            <if test="test1 != null and test1 != ''">
                and test1 = #{test1}
            </if>
            <if test="test2 != null and test2 != ''">
                and test2 = #{test2}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="sId" useGeneratedKeys="true">
        insert into sender(gid, s_address, s_phone, s_name, test1, test2)
        values (#{gid}, #{sAddress}, #{sPhone}, #{sName}, #{test1}, #{test2})
    </insert>

    <insert id="insertBatch" keyProperty="sId" useGeneratedKeys="true">
        insert into sender(gid, s_address, s_phone, s_name, test1, test2)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.gid}, #{entity.sAddress}, #{entity.sPhone}, #{entity.sName}, #{entity.test1}, #{entity.test2})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="sId" useGeneratedKeys="true">
        insert into sender(gid, s_address, s_phone, s_name, test1, test2)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.gid}, #{entity.sAddress}, #{entity.sPhone}, #{entity.sName}, #{entity.test1}, #{entity.test2})
        </foreach>
        on duplicate key update
        gid = values(gid),
        s_address = values(s_address),
        s_phone = values(s_phone),
        s_name = values(s_name),
        test1 = values(test1),
        test2 = values(test2)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update sender
        <set>
            <if test="gid != null">
                gid = #{gid},
            </if>
            <if test="sAddress != null and sAddress != ''">
                s_address = #{sAddress},
            </if>
            <if test="sPhone != null and sPhone != ''">
                s_phone = #{sPhone},
            </if>
            <if test="sName != null and sName != ''">
                s_name = #{sName},
            </if>
            <if test="test1 != null and test1 != ''">
                test1 = #{test1},
            </if>
            <if test="test2 != null and test2 != ''">
                test2 = #{test2},
            </if>
        </set>
        where s_id = #{sId}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from sender
        where s_id = #{sId}
    </delete>

</mapper>

